# server {
#     listen 80;
#     client_max_body_size 10M;

#     resolver 127.0.0.11 valid=30s;

#     location / {
#         root /usr/share/nginx/html;
#         index auth-frontend.html;
#         try_files $uri $uri.html $uri/ /auth-frontend.html;
#     }

#     location /api/auth/ {
#         set $auth_upstream auth_service:8081;

#         rewrite ^/api(/.*)$ $1 break;
#         proxy_pass http://$auth_upstream;

#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }

#     location /api/task/ {
#         set $task_upstream task_service:8082;

#         rewrite ^/api(/.*)$ $1 break;
#         proxy_pass http://$task_upstream;

#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }

server {
    listen 80;
    client_max_body_size 10M;

    resolver 127.0.0.11 valid=30s;
    root /usr/share/nginx/html;

    location = / {
        try_files /auth-frontend.html =404;
    }

    location /api/auth/ {
        set $auth_upstream auth_service:8081;
        rewrite ^/api(/.*)$ $1 break;
        proxy_pass http://$auth_upstream;

        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /api/task/ {
        set $task_upstream task_service:8082;
        rewrite ^/api(/.*)$ $1 break;
        proxy_pass http://$task_upstream;

        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location / {
        try_files $uri $uri.html =404;
    }
}